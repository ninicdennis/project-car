import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
	marginBot: { marginBottom: 4 },
	imageSizing: { maxWidth: 250, maxHeight: 250, marginLeft: 'auto', marginRight: 'auto' },
	marginTopBot: { marginTop: 8, marginBottom: 8 },
});
